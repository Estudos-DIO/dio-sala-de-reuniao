export class Sala {
    id!: number;
    nome!: string;
    data!: string;
    duracao!: number;

    // constructor( id: number, nome: string, data: string, duracao: number ) {
    //     this.id = id;
    //     this.nome = nome;
    //     this.data = data;
    //     this.duracao = duracao;
    // }

}